# 美术资源制作清单

给实际画图用的详细规格表。怎么把画好的图放进项目、怎么触发自动替换，见 [ART.md](ART.md)——那份是"how to"，这份是"具体每张图要画多大、放哪、什么格式"的清单。

## 通用规格（对所有图都适用）

| 项目 | 要求 |
|---|---|
| 格式 | **PNG**（需要透明通道就用 PNG-24/32；不透明的可以用 PNG-24） |
| 背景 | 除了"每关背景大图"这一类，其余所有图（角色/怪物/道具/砖块等）都要**透明背景**，不要留白色或彩色背景板，否则会变成一个方块贴在角色周围 |
| 色彩模式 | RGB / RGBA，8-bit 每通道即可，不需要额外的色彩管理（sRGB 默认即可） |
| 分辨率 | 不需要考虑"DPI"或视网膜倍图（@2x/@3x）——下表给的数字就是最终显示的物理像素尺寸，画多大最后就显示多大（会做拉伸/缩放匹配，但按下表尺寸画拉伸最小、最清晰） |
| 尺寸容差 | 不需要精确到个位数像素，但**宽高比**尽量接近下表数字，比例差太多会导致明显拉伸变形 |
| 风格方向 | Q 版萌向：大头小身、圆润轮廓、明快高饱和度配色，参考 [PLAN.md](PLAN.md) 第 2 节美术风格定位 |
| 命名 | 文件名必须和下表"文件名"列完全一致（大小写、下划线都要对，不能加多余后缀） |

---

## 一、玩家角色（2 张，必画）

| 文件名 | 存放路径 | 尺寸（px） | 说明 |
|---|---|---|---|
| `player_bunny.png` | `public/assets/sprites/` | **100 × 140** | P1「跳跳兔」。默认朝向：面朝右（角色向左走时程序会自动水平镜像，不用另画一张朝左的） |
| `player_cat.png` | `public/assets/sprites/` | **100 × 140** | P2「喵喵猫」。同上，面朝右 |

- 小/大形态目前共用同一张图（程序会把图纵向拉伸到"大形态"对应的高度）。如果以后想让大形态/喷火形态有专属画面，告诉我，我可以加对应的资源位。
- 角色受伤/无敌时会有闪烁和彩虹变色特效，这是程序自动加在图片上的滤镜效果，不需要额外画对应状态图。

---

## 二、怪物（8~11 张）

| 文件名 | 存放路径 | 尺寸（px） | 是否必画 | 说明 |
|---|---|---|---|---|
| `enemy_mochi.png` | `public/assets/sprites/` | **90 × 76** | 必画 | 团子怪，圆滚滚身形，建议头顶带蝴蝶结/触角类装饰 |
| `enemy_shellbuddy.png` | `public/assets/sprites/` | **90 × 83** | 必画 | 龟壳怪走路状态，圆润龟壳，建议糖果条纹配色 |
| `enemy_shellbuddy_shell.png` | `public/assets/sprites/` | **90 × 83** | 可选 | 龟壳怪缩进壳/被踢滑行时的画面（更扁平、更像一个壳）。不画这张也没关系，缩壳时会继续显示走路那张图 |
| `enemy_boss.png` | `public/assets/sprites/` | **158 × 145** | 必画 | Boss「糖果史莱姆王」（1-5），建议比小怪更有存在感、表情更夸张一些 |
| `enemy_iceshell.png` | `public/assets/sprites/` | **90 × 83** | 必画 | 冰晶龟壳（2-1），龟壳怪的雪原变体，冰蓝配色+白条纹 |
| `enemy_iceshell_shell.png` | `public/assets/sprites/` | **90 × 83** | 可选 | 冰晶龟壳缩壳/被踢滑行状态。不画则缩壳时继续显示走路那张 |
| `enemy_bat.png` | `public/assets/sprites/` | **104 × 69** | 必画 | 洞窟蝙蝠（2-2/2-3/2-4/2-5），倒挂+俯冲。画"展翅"一个造型即可；黑暗中的两颗红色眼点是程序叠加的（保证黑暗里可见），图里可以不画眼睛发光效果 |
| `enemy_gearmochi.png` | `public/assets/sprites/` | **90 × 76** | 必画 | 发条齿轮怪（2-3/2-5），团子怪的工厂变体，铁灰配色、带齿轮元素 |
| `enemy_sovereign_blue.png` | `public/assets/sprites/` | **158 × 145** | 可选 | 2-5 双子 Boss·蓝王（砸地留冰面）。不画则自动用 `enemy_boss.png` 染成蓝色 |
| `enemy_sovereign_red.png` | `public/assets/sprites/` | **158 × 145** | 可选 | 2-5 双子 Boss·红王（砸地喷火）。不画则自动用 `enemy_boss.png` 染成红色 |
| `enemy_hopper.png` | `public/assets/sprites/` | **84 × 72** | 必画 | 跳跳怪（3-1），静止蹲伏 + 蓄力跳跃，画一个"预备起跳"的蹲姿造型即可，蓄力时的白闪/腾空姿态是程序处理 |
| `enemy_shyghost.png` | `public/assets/sprites/` | **96 × 96** | 必画 | 害羞幽灵（3-2），白色圆团+黑眼+腮红，建议画"睁眼看着前方"的常态；被注视时捂脸/半透明变化是程序自动处理，不需要另画捂脸帧 |
| `enemy_turret.png` | `public/assets/sprites/` | **120 × 132** | 可选 | 峡谷炮塔（3-3），固定不动的石堡小塔，只需要画一个"待机"造型；开火没有专属动画（炮弹从塔顶飞出即代表开火），受击闪红是程序叠加的滤镜 |

- 同样默认朝向面朝右，自动镜像处理向左走的情况。
- Boss 被踩中/攻击预警时的颜色闪烁是程序叠加的滤镜特效，不需要额外画变色图。

---

## 三、道具（5 张，必画）

| 文件名 | 存放路径 | 尺寸（px，正方形，图案居中） | 说明 |
|---|---|---|---|
| `item_coin.png` | `public/assets/sprites/` | **54 × 54** | 金币 |
| `item_mushroom.png` | `public/assets/sprites/` | **66 × 66** | 蘑菇（吃了变大） |
| `item_fireflower.png` | `public/assets/sprites/` | **60 × 60** | 火焰花（吃了能喷火） |
| `item_star.png` | `public/assets/sprites/` | **60 × 60** | 无敌星 |
| `item_lantern.png` | `public/assets/sprites/` | **60 × 60** | 萤火提灯（2-2/2-5 黑暗关，拾取后视野光圈变大），建议自带发光感的暖色调 |

---

## 四、砖块与问号砖（2~3 张）

| 文件名 | 存放路径 | 尺寸（px） | 是否必画 | 说明 |
|---|---|---|---|---|
| `block_brick.png` | `public/assets/sprites/` | **96 × 96** | 必画 | 普通砖块（顶碎前的样子） |
| `block_question.png` | `public/assets/sprites/` | **96 × 96** | 必画 | 未使用的问号砖 |
| `block_question_used.png` | `public/assets/sprites/` | **96 × 96** | 可选 | 已弹出道具后的问号砖（一般画得更暗/更旧）。不画的话程序会用"变暗一个颜色"代替 |

---

## 五、其它游戏物件（14~15 张）

| 文件名 | 存放路径 | 尺寸（px） | 说明 |
|---|---|---|---|
| `fireball.png` | `public/assets/sprites/` | **42 × 42** | 火焰形态发射的火球，正方形图，圆形图案居中 |
| `bubble.png` | `public/assets/sprites/` | **108 × 108** | 双人模式复活泡泡，正方形图，圆形图案居中 |
| `platform.png` | `public/assets/sprites/` | **288 × 96**（会被拉伸/压缩到每个具体平台的实际宽度，不同关卡的移动平台宽度不一样） | 移动平台，建议画一个耐拉伸的简单图案（比如云朵形状的浮板、木板纹理），不要画太精细的花纹，拉伸后会变形 |
| `cloud.png` | `public/assets/sprites/` | **150 × 90** | 背景云朵，会有轻微缩放变化制造远近层次感 |
| `flagpole.png` | `public/assets/sprites/` | **18 × 864** | 每关终点旗杆的杆身，细长条，建议画一个可以纵向拉伸不违和的杆子花纹（比如简单的渐变或竖纹） |
| `flag.png` | `public/assets/sprites/` | **132 × 72** | 旗杆顶端的小旗子 |
| `pipe.png` | `public/assets/sprites/` | **192 × 288**（会拉伸/压缩到具体这根水管的实际宽高，不同水管长短不一样） | 绿色水管整体（含顶部比身体宽一圈的"管口"），一张图涵盖整根水管，不需要分开画管口和管身 |
| `pipe_trap.png` | `public/assets/sprites/` | **120 × 108** | 水管里的机械夹子，会从管口升起/缩回，画一个"关闭状态"的夹子/钳子造型即可，不需要画张合动画（升降本身就是动画） |
| `switch_plate.png` | `public/assets/sprites/` | **84 × 18** | 踩踏式机关的地面感应板，双人合作"同时踩两块板开宝箱"（1-3）和"踩板限时开门"（1-4/1-5）这两种机关**共用同一张图**，触发后会变色（代码自动叠加一层染色，不需要另画"已触发"的版本） |
| `chest.png` | `public/assets/sprites/` | **84 × 66** | 双人合作机关（1-3）里两块感应板同时踩下后弹开的宝箱，开启瞬间同样是代码自动加一层高亮染色，不需要另画"开启后"的版本 |
| `door.png` | `public/assets/sprites/` | **42 × 384**（会拉伸/压缩到具体这扇门的实际高度，不同关卡的门高不完全一样） | 踩板限时开门机关（1-4/1-5/2-2）里挡路的门，关闭状态挡住通路，开启期间隐藏，只需要画"关闭"这一个造型 |
| `flame_jet.png` | `public/assets/sprites/` | **60 × 288** | 喷火柱的火焰柱身（2-3/2-5），竖长条向上喷的火焰造型，画"全喷发"一帧即可——升起/熄灭/闪烁都是程序控制的显示切换，喷口底座仍是程序绘制 |
| `belt.png` | `public/assets/sprites/` | **96 × 30**（**必须左右无缝平铺**，同 `ground_tile` 的要求） | 传送带表面（2-3/2-5），会沿带面横向重复平铺并随传送方向滚动，画箭头/纹路时注意左右边缘衔接 |
| `crumble_platform.png` | `public/assets/sprites/` | **288 × 96**（会拉伸到 2~3 格宽的实际尺寸） | 碎裂平台完好状态（2-4/2-5），建议画成带细小裂纹的悬浮石板，和普通平台有可辨识的区别 |
| `crumble_platform_cracked.png` | `public/assets/sprites/` | **288 × 96** | 可选。踩上后 0.4 秒摇晃期显示的"裂纹加深"状态，不画则摇晃期沿用完好图（仍有闪烁提示） |
| `spring_small.png` | `public/assets/sprites/` | **168 × 36** | 小弹簧（3-1，弹起 5 格）。矮而扁的踏板造型——身高不能画得太高（参考身高比例：远比角色矮），否则弹跳判定容易读成"撞墙"而不是"踩上去弹起" |
| `spring_big.png` | `public/assets/sprites/` | **168 × 36** | 大弹簧（3-1，弹起 8 格），同小弹簧尺寸，建议用更醒目的配色/花纹区分"这个弹得更高" |
| `switch_block_red.png` | `public/assets/sprites/` | **96 × 96**（会拉伸/压缩到具体这一段方块的实际宽度，同 `platform.png` 的用法） | 红蓝切换方块·红色态（3-2）。非激活色时程序会整体调低透明度（轮廓仍可见），不需要另画"虚化"版本 |
| `switch_block_blue.png` | `public/assets/sprites/` | **96 × 96**（同上，会拉伸） | 红蓝切换方块·蓝色态（3-2） |
| `turret_shot.png` | `public/assets/sprites/` | **72 × 72** | 炮塔炮弹（3-3），正方形图，圆形图案居中，落地即碎不需要碎裂帧 |

---

## 六、地面贴图（1 张，**必须无缝平铺**）

| 文件名 | 存放路径 | 尺寸（px） | 说明 |
|---|---|---|---|
| `ground_tile.png` | `public/assets/sprites/` | **96 × 96** | 地面贴图。**关键要求：左边缘和右边缘的花纹必须能无缝拼接**——这张图不是被拉伸使用的，是按 96×96 一块一块重复贴满整个地面，所以左右边缘对不上会看出明显接缝。上边缘可以画草地/沙地/糖果表面等装饰，下边缘不用管（会被贴在地下看不到） |

---

## 七、每关背景大图（10 张，可选，一次性整体换皮）

如果想直接换掉现在的天空蓝底+程序化云朵观感，画一张贯穿整关的背景大图。**这一类背景图不需要透明背景**，可以是完整的不透明画面（因为背景本来就在最底层）。

| 文件名 | 存放路径 | 尺寸（px，宽 × 高，已按关卡实际长度精确计算） | 关卡名 |
|---|---|---|---|
| `bg-1-1.png` | `public/assets/backgrounds/` | **10368 × 960** | 阳光草地 |
| `bg-1-2.png` | `public/assets/backgrounds/` | **13440 × 1152** | 蘑菇森林 |
| `bg-1-3.png` | `public/assets/backgrounds/` | **14400 × 1536** | 云端漫步 |
| `bg-1-4.png` | `public/assets/backgrounds/` | **16800 × 1536** | 沙丘迷城 |
| `bg-1-5.png` | `public/assets/backgrounds/` | **17760 × 1344** | 糖果城堡 |
| `bg-2-1.png` | `public/assets/backgrounds/` | **23040 × 1152** | 雪晶滑原 |
| `bg-2-2.png` | `public/assets/backgrounds/` | **21120 × 1344** | 萤光洞窟（本关有黑暗遮罩叠加在最上层，背景建议画深色调） |
| `bg-2-3.png` | `public/assets/backgrounds/` | **23424 × 1152** | 熔岩工厂 |
| `bg-2-4.png` | `public/assets/backgrounds/` | **6144 × 4224**（纵版塔楼：窄而高，和其它关方向相反） | 狂风高塔 |
| `bg-2-5.png` | `public/assets/backgrounds/` | **23616 × 1344** | 双子魔堡 |
| `bg-3-1.png` | `public/assets/backgrounds/` | **23040 × 1344** | 弹簧牧场 |
| `bg-3-2.png` | `public/assets/backgrounds/` | **23040 × 1344** | 红蓝回廊 |
| `bg-3-3.png` | `public/assets/backgrounds/` | **28416 × 1152** | 疾风飞车 |

- 这几张图非常宽（横向卷轴关卡的全长），建议先画一个较窄的"可重复/可延展"的构图打底，再用软件里的画布拉伸/平铺工具铺到目标尺寸，不需要真的从头到尾手绘一张上万像素宽的图。
- 只画其中几关也可以，没画背景大图的关卡会继续显示现在的天空+云朵效果，互不影响。
- 这张图只影响天空/远景，**地面本身（`ground_tile.png`）、角色、怪物、道具不受影响**，会正常叠加显示在背景大图上面。

---

## 汇总：需要建的文件夹

```
public/assets/sprites/       ← 除背景大图外的所有图都放这里
public/assets/backgrounds/   ← 只有"每关背景大图"放这里
```

两个文件夹如果不存在，创建后直接把文件丢进去即可，不需要额外配置。

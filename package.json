{
  "name": "super-mario-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "audit": "node tools/audit-levels.mjs",
    "test": "node --import ./tests/register.mjs --test \"tests/*.test.mjs\"",
    "check": "npm run audit && npm test && npm run build"
  },
  "dependencies": {
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "vite": "^7.0.0"
  }
}